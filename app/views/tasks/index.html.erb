<h1>Here are your tasks</h1>
<% @tasks.each do |task| %>
  <p><%= link_to task.title, task_path(task) %>
  - <%= task.completed %> - <%= link_to "Delete", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<% end %>
<p><%= link_to "Add a task", new_path %></p>
